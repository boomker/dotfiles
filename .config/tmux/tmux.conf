unbind C-b
set -g prefix C-g

set -g status-interval 2
set -g status-position top
set -g status-justify centre
set -g status-left-length 100
set -g status-right-length 100

set -gqw utf8 on
set -g focus-events on
set -g set-clipboard on
set -g renumber-windows on
set -g visual-bell off
set -g visual-silence off
set -g visual-activity off
set -g monitor-activity on
set -g allow-passthrough on

set -gw mode-keys vi
set -g base-index 1
set -g pane-base-index 1
set -g escape-time 20
set -g repeat-time 200
set -g display-time 1000
set -g history-limit 10000
set -g default-shell "${SHELL}"

set -ga cursor-style bar
set -ga popup-border-lines "rounded"
set default-terminal "tmux-256color" # truecolor support
set -ga terminal-overrides ',*:Ss=\E[%p1%d q:Se=\E[2 q'
set -ga terminal-features ",ghostty:RGB,ghostty:usstyle,*256col*:Tc"
set-hook -g pane-focus-in 'run-shell "printf \"\\033[6 q\\033[5 q\" > #{pane_tty}"'
set-hook -g after-new-window 'run-shell "printf \"\\033[6 q\\033[5 q\" > #{pane_tty}"'
set-hook -g after-split-window 'run-shell "printf \"\\033[6 q\\033[5 q\" > #{pane_tty}"'
set -ga update-environment TERM
set -ga update-environment TERM_PROGRAM
set -g automatic-rename-format "#{window_icon}"

bind-key c copy-mode
bind-key o next-layout
bind-key n next-window
bind-key u last-window
bind-key r rotate-window
bind-key b previous-window
bind-key t new-window -c "#{pane_current_path}"
bind-key '-' split-window -c "#{pane_current_path}"
bind-key '|' split-window -h -c "#{pane_current_path}"
bind-key M set mouse \; display-message "Mouse: #{?mouse,ON,OFF}"
bind-key P run-shell "~/.config/tmux/plugins/fzf-tmux-pane/fjtpane.sh"
bind-key R source-file ~/.tmux.conf \; display-message "Reload config success!"
bind-key G pipe-pane -o "cat >>~/Desktop/#S_#W.log" \; display-message "Toggled logging to ~/Desktop/#S_#W.log"
bind-key S set-window-option synchronize-panes\; display-message "synchronize-panes is now #{?pane_synchronized,on,off}"

bind-key F if-shell -F '#{==:#{session_name},popup}' {
    detach-client
} {
    if-shell -F '#{e|==:#{N/s:popup},0}' {
        new-session -d -s 'popup' "/opt/homebrew/bin/fish"
  }
    display-popup -w 80% -h 80% -E "tmux attach-session -t popup"
}

bind-key -T copy-mode-vi c send-keys -X clear-selection
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi r send-keys -X rectangle-toggle
bind-key -T copy-mode-vi K send-keys -N 5 -X cursor-up
bind-key -T copy-mode-vi J send-keys -N 5 -X cursor-down
bind-key -T copy-mode-vi L send-keys -X end-of-line
bind-key -T copy-mode-vi H send-keys -X start-of-line
bind-key -T copy-mode-vi Y send-keys -X copy-end-of-line
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

bind -r > swap-pane -D
bind -r < swap-pane -U    # swap current pane with the previous one
bind -r h select-pane -L  # move left
bind -r j select-pane -D  # move down
bind -r k select-pane -U  # move up
bind -r l select-pane -R  # move right
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# tmux plugin manager tpm and plugins lists: {
run-shell '~/.local/bin/plux'
# set -g @plux_toml_path "~/.config/tmux/plux.toml"
# set -g @plux_plugins_path "~/.config/tmux/plugins/"
# set -g @plugin 'dracula-theme'
# set -g @plugin 'tmux-resurrect'
# set -g @plugin 'tmux-continuum'
# set -g @plugin 'tmux-nerd-name'
# set -g @plugin 'laktak/extrakto'
# }

set -g @fingers-key "g"
# set -g @extrakto_key "g"
# set -g @extrakto_copy_key "ctrl-y"
# set -g @extrakto_grab_key "ctrl-x"
# set -g @extrakto_insert_key "enter"
# set -g @extrakto_grab_area recent
# set -g @extrakto_popup_size 77%,77%

# dracula config: https://github.com/dracula/tmux/blob/master/docs/CONFIG.md
# https://github.com/dracula/tmux/blob/master/docs/color_theming/catppuccin.md
set -g @dracula-plugins "battery network"
set -g @dracula-show-flags true
set -g @dracula-military-time true
set -g @dracula-show-timezone false
set -g @dracula-show-powerline true
set -g @dracula-border-contrast true
set -g @dracula-show-left-sep 
set -g @dracula-show-right-sep 
set -g @dracula-show-left-icon "#S"
set -g @dracula-battery-label "🔋"
set -g @dracula-network-wifi-label "🛜"
set -g @dracula-network-offline-label "󱍢 "
set -g @dracula-network-ethernet-label "󰈀 "
set -g @dracula-ping-server "douyin.com"
set -g @dracula-network-hosts "douyin.com"
set -g @dracula-powerline-bg-transparent true
set -g @dracula-show-ssh-only-when-connected true
set -g @dracula-colors "
Rosewater='#f4dbd6'
Flamingo='#f0c6c6'
Pink='#f5bde6'
Mauve='#c6a0f6'
Red='#ed8796'
Maroon='#ee99a0'
Peach='#f5a97f'
Yellow='#eed49f'
Green='#a6da95'
Teal='#8bd5ca'
Sky='#91d7e3'
Sapphire='#7dc4e4'
Blue='#8aadf4'
Lavender='#b7bdf8'
Text='#cad3f5'
Subtext1='#b8c0e0'
Subtext0='#a5adcb'
Overlay2='#939ab7'
Overlay1='#8087a2'
Overlay0='#6e738d'
Surface2='#5b6078'
Surface1='#494d64'
Surface0='#363a4f'
Base='#24273a'
Mantle='#1e2030'
Crust='#181926'
"

# set -g @resurrect-processes ':all:'
set -g @resurrect-strategy-vim 'session'
set -g @resurrect-strategy-nvim 'session'
set -g @resurrect-capture-pane-contents 'on'
set -g @resurrect-processes 'ssh tssh codex claude opencode'

set -g @continuum-boot 'on'
set -g @continuum-restore 'on'
set -g @continuum-save-interval '30'
set -g @continuum-boot-options 'fullscreen'
